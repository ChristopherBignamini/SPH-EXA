
if (SPH_EXA_WITH_GRACKLE OR SPH_EXA_WITH_CHANGA_COOLING)
    add_subdirectory(cooling)
endif ()
if (SPH_EXA_WITH_DISKS)
    add_subdirectory(Disk)
endif ()
if (SPH_EXA_WITH_TDE_INIT)
    add_subdirectory(polytrope)
endif()
